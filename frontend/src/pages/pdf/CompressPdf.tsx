// ============================================================
// CompressKro — Compress PDF Panel Component
// Renders inside the PdfWorkspaceShell's right options panel.
// No ToolPageLayout — SEO is handled by the shell.
// ============================================================

import PDFCompressor from '../../components/PDFCompressor/PDFCompressor';

export function CompressPdf() {
  return <PDFCompressor />;
}
export default CompressPdf;
